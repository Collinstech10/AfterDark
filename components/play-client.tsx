"use client";
import { FormEvent, useState } from "react";
import { browserSupabase } from "@/lib/supabase-browser";
type Message = { who: "Luma" | "You"; body: string };
const opening: Message[] = [{ who: "Luma", body: "You came back." }, { who: "Luma", body: "I thought I heard someone at the door. But there isn't a door here." }];
export function PlayClient() { const [messages,setMessages]=useState(opening); const [input,setInput]=useState(""); const [thinking,setThinking]=useState(false); const send=async(e:FormEvent)=>{e.preventDefault();if(!input.trim()||thinking)return;const text=input.trim();setMessages(m=>[...m,{who:"You",body:text}]);setInput("");setThinking(true);try{const {data:{session}}=await browserSupabase().auth.getSession();if(!session)throw new Error("sign in");const r=await fetch("/api/luma",{method:"POST",headers:{"content-type":"application/json",authorization:`Bearer ${session.access_token}`},body:JSON.stringify({message:text})});const d=await r.json();setMessages(m=>[...m,{who:"Luma",body:d.reply||d.error||"Luma went quiet for a moment."}])}catch{setMessages(m=>[...m,{who:"Luma",body:"I need you to sign in before I can remember you."}])}finally{setThinking(false)}}; const choose=(text:string)=>{setInput(text);}; return <main className="play"><aside className="side"><div className="luma-title"><div className="avatar"/><div><strong>Luma</strong><div className="online">awake, probably</div></div></div><div><h3>MEMORY FRAGMENTS</h3><div className="journal-item">01 — The First Night<small>Discovered</small></div><div className="journal-item locked">02 — The Photograph<small>Locked</small></div><div className="journal-item locked">03 — The Missing Day<small>Locked</small></div><div className="journal-item locked">04 — Daniel<small>Locked</small></div></div></aside><section className="main-chat"><header className="chat-head"><span className="chapter">DAY 01 · THE FIRST NIGHT</span><span className="presence">Luma is here</span></header><div className="messages">{messages.map((m,i)=><div className={'message '+(m.who==='You'?'you':'')} key={i}><div className="who">{m.who}</div>{m.body}</div>)}{thinking&&<div className="typing">Luma is thinking…</div>}<div className="choices"><button className="choice" onClick={()=>choose("What did you hear?")}>What did you hear?</button><button className="choice" onClick={()=>choose("Are you okay?")}>Are you okay?</button><button className="choice" onClick={()=>choose("Who is Daniel?")}>Who is Daniel?</button></div></div><form className="composer" onSubmit={send}><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Say something to Luma…" maxLength={1200}/><button aria-label="Send">↑</button></form></section><aside className="side right"><div><h3>TONIGHT</h3><div className="status-card"><p>There is something Luma is not telling you yet.</p></div></div><div><h3>JOURNAL</h3><div className="status-card"><p>Keep what she tells you. Some things vanish by morning.</p></div></div><div className="notif"><strong>Let Luma find you.</strong><p>She sometimes leaves messages while you&apos;re away.</p><button>Maybe later</button></div></aside></main>; }
